/** process initializations in for contorl **/


class JavaForInit { }/* extends FieldDec{
	FieldDec(JavaScope scope, Node n){
		super(scope,n);
       }
	public void visitDeclarator(GNode n) {
		//Declarators always come after Modifiers and Type in our java AST
		//** add declared fields to basic for control **
		scope.addField = new JavaField(this.isStatic, this.modifiers, this.type, this.dimensions, this.scope, (Node)n);
		//this.fields.add(field);
	}
}
*/
